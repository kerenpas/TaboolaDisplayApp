<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <permission
        android:name="com.example.tabooladisplayapp.CHANGE_CELL_COLOR"
        android:protectionLevel="signature" />

    <application
        android:name=".TaboolaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaboolaDisplayApp">
        <service
            android:name=".presentation.service.CellColorService"
            android:exported="true"
            android:permission="com.example.tabooladisplayapp.CHANGE_CELL_COLOR">
            <intent-filter>
                <action android:name="com.example.tabooladisplayapp.action.CHANGE_CELL_COLOR" />
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".data.provider.CellColorContentProvider"
            android:authorities="com.example.tabooladisplayapp.cellcolorprovider"
            android:exported="true"
            android:permission="com.example.tabooladisplayapp.CHANGE_CELL_COLOR" />
    </application>

</manifest>
